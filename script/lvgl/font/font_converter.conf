# Font Converter Configuration (Core standalone)
# Paths relative to project root

# Source TTF/OTF fonts
FONT_SOURCE_DIR="asset/font"

# Output directory (data/ subfolder created automatically)
FONT_OUTPUT_DIR="src/ui/shared/font"

# Character range - ASCII + Latin1 only (no PUA icons)
CHAR_RANGE="ASCII,LATIN1"
